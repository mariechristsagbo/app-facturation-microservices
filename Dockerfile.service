FROM node:22-alpine

WORKDIR /app
ENV NODE_ENV=production

COPY package.json package-lock.json ./
COPY apps/frontend/package.json apps/frontend/package.json
RUN npm ci --omit=dev

COPY shared shared
COPY services services

CMD ["node", "services/client-service/server.js"]
